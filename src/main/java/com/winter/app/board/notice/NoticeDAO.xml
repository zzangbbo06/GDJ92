<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <!-- namespace의 이름은 사용하려는 DAO의 풀패키지명과 동일하게 작성 -->
<mapper namespace="com.winter.app.board.notice.NoticeDAO">
	<!-- ID는 DAO의 메서드명과 동일하게 작성 -->
	
	<!-- <insert id="add" parameterType="NoticeVO">
		INSERT INTO NOTICE(BOARD_TITLE, BOARD_CONTENTS,BOARD_WRITER, BOARD_DATE, BOARD_HIT)
		VALUES(#{boardTitle}, #{boardContents}, #{boardWriter}, now(), 0)
	</insert> -->
	
	
	<select id="list" resultType="NoticeVO">
		SELECT *
		FROM NOTICE
		ORDER BY BOARD_NUM DESC	
	</select>
	
	<select id="detail" parameterType="NoticeVO" resultType="NoticeVO">
		SELECT *
		FROM NOTICE
		WHERE BOARD_NUM=#{boardNum}
		<!-- #{} 여기에 들어갈 parameterType을 적어라 -->
	</select>
	
	<insert id="insert" parameterType="NoticeVO">
		INSERT INTO NOTICE(BOARD_NUM, BOARD_TITLE, BOARD_CONTENTS, BOARD_WRITER, BOARD_DATE, BOARD_HIT)
		VALUES
		(NULL, #{boardTitle}, #{boardContents}, #{boardWriter}, now(), 0)
	</insert>

	<update id="update" parameterType="NoticeVO">
		UPDATE NOTICE
		SET
			BOARD_TITLE = #{boardTitle},
			BOARD_CONTENTS = #{boardContents},
			BOARD_WRITER = #{boardWriter}
		WHERE BOARD_NUM = #{boardNum}	
	</update>
	
	<delete id="delete" parameterType="NoticeVO">
		DELETE FROM NOTICE
		WHERE BOARD_NUM = #{boardNum}
	</delete>

</mapper>